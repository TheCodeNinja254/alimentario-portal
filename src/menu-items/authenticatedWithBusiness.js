import dashboard from "./dashboard";
import other from "./other";
import accountAuthenticatedWithBusiness from "./accountAuthenticatedWithBusiness";

const authenticatedWithBusinessMenuItems = {
  items: [dashboard, accountAuthenticatedWithBusiness, other],
};

export default authenticatedWithBusinessMenuItems;
