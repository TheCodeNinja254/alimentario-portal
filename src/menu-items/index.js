import dashboard from "./dashboard";
import pages from "./pages";
// import utilities from './utilities'; -- To be included as needed
import other from "./other";

// ===========================|| MENU ITEMS ||=========================== //

const menuItems = {
  items: [dashboard, pages, other],
};

export default menuItems;
